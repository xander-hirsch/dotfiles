# vim: filetype=bash

HOME_BIN="${HOME}/.bin" 
if [ -d $HOME_BIN ]; then
	PATH="${HOME_BIN}:$PATH"
fi

LOCAL_BIN="${HOME}/.local/bin"
if [ -d $LOCAL_BIN ] ; then
	PATH="${LOCAL_BIN}:$PATH"
fi

if [ -f ~/.bashrc ]; then source ~/.bashrc; fi

